# macOS 产品线规划文档（DOCS）

本目录为 **macOS 专属运维面板** 的产品与技术规划区，对应评审中的「DOCS」目录。

> **路径说明**：仓库根目录 `docs/` 同时存放官方多语言 README。在大小写不敏感的文件系统（默认 macOS）上无法与 `DOCS/` 并存为两个目录，故规划文档统一放在 **`docs/macos-panel/`**。在大小写敏感的 Linux 环境中，若需可再增加顶层 `DOCS/` 符号链接指向此处。

| 文档 | 说明 |
|------|------|
| [prd.md](./prd.md) | 产品需求说明（PRD）与技术方案 v0.1 |
